# Best-effort optional packages — failures do not abort pip-deps install.
# WSL bootstrap installs colossalai, tensornvme, flash-attn after cmake/build-essential/libaio-dev.
xformers; platform_system == "Windows"
triton; platform_system == "Linux"
colossalai; platform_system == "Linux"
# Open-Sora ckpt.py imports tensornvme (training async I/O); inference uses a stub when import fails.
tensornvme; platform_system == "Linux"
# Open-Sora mmdit attention; requires torch + CUDA toolkit in WSL — stub uses PyTorch SDPA when missing.
flash-attn; platform_system == "Linux"
