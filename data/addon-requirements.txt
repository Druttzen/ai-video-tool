# AI Video Creator — managed addon pip requirements (auto-synced to userData/addons/requirements.txt)
# torch/torchvision/torchaudio installed separately in addon-updater (CUDA index on Windows when available).
# opensora installed via pip install -e on managed Open-Sora clone — NOT from PyPI.

einops
accelerate>=0.25.0
transformers>=4.40.0
diffusers>=0.25.0
safetensors
huggingface-hub>=0.20.0
open-clip-torch

opencv-python
imageio
imageio-ffmpeg
pillow
decord; platform_system != "Windows"
moviepy
ffmpeg-python
av
librosa
soundfile

gradio>=4.0.0
fastapi
uvicorn
python-multipart
aiofiles

tqdm
numpy
scipy
pyyaml
regex
packaging
requests
matplotlib
psutil
pandas
scikit-learn
sentencepiece
protobuf
ftfy
tiktoken

pip>=24.0
setuptools>=69.0
wheel
virtualenv

nvidia-ml-py; platform_system != "Darwin"
